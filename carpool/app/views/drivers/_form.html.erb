<%= form_for(@driver) do |f| %>
  <% if @driver.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@driver.errors.count, "error") %> prohibited this driver from being saved:</h2>

      <ul>
      <% @driver.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :time %><br>
    <%= f.time_select :time %>
  </div>
  <div class="field">
    <%= f.label :sourceposition %><br>
    <%= f.text_field :sourceposition %>
  </div>
  <div class="field">
    <%= f.label :destinationposition %><br>
    <%= f.text_field :destinationposition %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
